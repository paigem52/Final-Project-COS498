<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feed Page</title>
    <link rel="stylesheet" href="/css/style.css">

</head>
<body>
    {{> nav }}

    <main>
        <h1>All Comments</h1>
        {{#if comments.length}}
            <ul>
                {{#each comments}}
                    <li><strong>{{display_name}}</strong>: {{text}} ({{createdAt}})</li>
                {{/each}}
            </ul>
        {{else}}
            <p>No comments yet</p>
        {{/if}}
    </main>

    {{> footer}}
</body>
</html>

